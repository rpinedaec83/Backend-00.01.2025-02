<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ecommerce Auth</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    .nav-btn {
      margin-right: 8px;
      border-radius: 20px;
      padding: 8px 15px;
      transition: all 0.3s ease;
      font-weight: 500;
    }
    .nav-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
    .btn-home {
      background-color: #f8f9fa;
      color: #212529;
    }
    .btn-cart {
      background-color: #4e73df;
      color: white;
    }
    .btn-orders {
      background-color: #1cc88a;
      color: white;
    }
    .btn-sell {
      background-color: #f6c23e;
      color: #212529;
      font-weight: 600;
    }
    .btn-sell:hover {
      background-color: #f4b619;
      color: #212529;
    }
    .navbar-text {
      color: #2e59d9;
      font-weight: 600;
    }
    .logout-btn {
      border-radius: 20px;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
      <a class="navbar-brand" href="/"><strong>Guru Tech Store</strong></a>
      <div class="navbar-nav">
        <% if (!user) { %>
          <a class="nav-link" href="/login">Login</a>
          <a class="nav-link" href="/register">Registro</a>
        <% } else { %>
          <div class="d-flex align-items-center">
            <!-- Botones adicionales para usuarios logueados -->
            <a href="/" class="btn btn-home nav-btn me-2">
              <i class="fas fa-home"></i> Home
            </a>
            <a href="/carrito" class="btn btn-cart nav-btn me-2">
              <i class="fas fa-shopping-cart"></i> Carrito
            </a>
            <a href="/carrito/mis-ordenes" class="btn btn-orders nav-btn me-2">
              <i class="fas fa-clipboard-list"></i> Mis Órdenes
            </a>
            <a href="/agregar-producto" class="btn btn-sell nav-btn me-2">
              <i class="fas fa-plus-circle"></i> Vender
            </a>
            
            <span class="navbar-text me-3">
              <i class="fas fa-user-circle"></i> Hola, <%= user.nombre %> <%= user.apellido %>
            </span>
            <a class="btn btn-outline-danger logout-btn" href="/logout">
              <i class="fas fa-sign-out-alt"></i> Salir
            </a>
          </div>
        <% } %>
      </div>
    </div>
  </nav>
  <div class="container mt-4">
    <% if (locals.success_msg) { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= success_msg %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>
    <% if (locals.error_msg) { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <%= error_msg %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>