
function sumOfCubes(num1,num2,num3){
    let exponente=3;
    return Math.pow(num1,exponente)+Math.pow(num2,exponente)+Math.pow(num3,exponente);
}

const sumaDeCubos = (num1,num2,num3) => {
    let exponente=3;
    return Math.pow(num1,exponente)+Math.pow(num2,exponente)+Math.pow(num3,exponente);
}
resultado = sumaDeCubos(2,3,4);

console.log(resultado);