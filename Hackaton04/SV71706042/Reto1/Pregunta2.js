
function potenciaNumero(numero,exponente){
    return Math.pow(numero,exponente)
}

//Es igual que poner
const potenciaDeNumero = (numero,exponente) => Math.pow(numero,exponente);

resultado = potenciaDeNumero(2,6);

console.log(resultado);